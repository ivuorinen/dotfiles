# x-datetime

Print the current local date and time as `YYYY-MM-DD HH:MM:SS`.

## Usage

```bash
x-datetime
```

Also available as `x datetime` via the `x` dispatcher.

<!-- vim: set ft=markdown spell spelllang=en_us cc=80 : -->
