#!/bin/sh
#USAGE about "Check SSL certificate expiry date"
#USAGE flag "-p <port>" help="Port number (default: 443)"
#USAGE flag "-d <days>" help="Days threshold for expiry warning"
#USAGE arg "<hostname>" help="Domain or hostname to check"
#
# About
# -----
# Check the expiry date of the SSL certificate on the given host.
#
# License
# -------
# Copyright (c) 2013-2015 by Steve Kemp.  All rights reserved.
#
# This script is free software; you can redistribute it and/or modify it under
# the same terms as Perl itself.
#
# The LICENSE file contains the full text of the license.

# Simple function to show usage information, and exit.
usage()
{
  echo "Usage: $0 [-d] [-p 443] domain1.org domain2.com .. domainN"
  exit 0
}

# Default settings for flags set by the command-line arguments
days=0
port=443

# Parse the argument(s) - i.e. look for "-d" / "-p 443".
while getopts "h?dp:" opt; do
  case $opt in
    h)
      usage
      ;;
    \?)
      usage
      ;;
    d)
      days=1
      ;;
    p)
      port=$OPTARG
      ;;
  esac
done
shift $((OPTIND - 1))

# Ensure we have some arguments
if [ "$#" = "0" ]; then
  usage
fi

# Make a temporary file, reused for every domain
# Try GNU syntax first, fall back to BSD
if tmp=$(mktemp 2> /dev/null) && [ -f "$tmp" ]; then
  # GNU mktemp succeeded
  :
else
  # Try BSD syntax
  tmp=$(mktemp -t tmp)
fi

# Always clean up the temp file, even on Ctrl+C between domains.
trap 'rm -f "$tmp"' EXIT INT TERM HUP

# For each domain-name on the command-line.
for name in "$@"; do

  # Download the certificate
  if ! echo "" | openssl s_client -connect "$name:$port" > "$tmp" 2> /dev/null; then
    echo "Failed to get cert from https://$name:$port/"
    exit 3
  fi

  # Get the expiry date ("notAfter=Mar 14 12:00:00 2026 GMT")
  date=$(openssl x509 -in "$tmp" -noout -enddate)
  date=${date#*=}

  # Convert the expiry date + todays date to seconds-past epoch
  # Try GNU syntax first, fall back to BSD
  if then=$(date --date "$date" +%s 2> /dev/null); then
    # GNU date succeeded
    :
  else
    # BSD date requires manual parsing; split "Mar 14 12:00:00 2026 GMT"
    # on whitespace (the for-loop list is already expanded, so reusing
    # the positional parameters here is safe)
    # shellcheck disable=SC2086
    set -- $date
    month=$1
    day=$2
    year=$4
    hour=${3%%:*}
    rest=${3#*:}
    minute=${rest%%:*}
    second=${rest#*:}
    then=$(date -v"${year}"y -v"${month}" -v"${day}"d -v"${hour}"H -v"${minute}"M -v"${second}"S -u +%s)
  fi

  now=$(date +%s)

  # Day diff (no quotes inside $((..)) — dash rejects quoted operands)
  diff=$((then - now))
  diff=$((diff / 86400))

  # All done
  if [ "$days" = "1" ]; then
    echo "${name}: ${diff}"
  else
    echo "$name"
    echo "    Expires: ${date}"
    echo "    Days: ${diff}"
  fi
done
