#!/usr/bin/env bash
#USAGE about "Show external IP address"
#
# Display the external IP address via OpenDNS. Also reachable as `x ip`.

set -euo pipefail

if ! command -v dig &> /dev/null; then
  echo "Error: dig is required" >&2
  exit 1
fi

dig +short myip.opendns.com @resolver1.opendns.com
