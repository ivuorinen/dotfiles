# x-quota-usage

Display filesystem quota usage in a human-readable table.

## Usage

```bash
x-quota-usage
```

Runs the `quota` command and formats the output as a table showing mount
point, usage percentage, used and total space in gigabytes, and a
progress bar.

<!-- vim: set ft=markdown spell spelllang=en_us cc=80 : -->
