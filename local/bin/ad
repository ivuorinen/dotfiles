#!/usr/bin/env bash
# age decrypt file with github keys

if ! x-have age; then
  print "age not installed, could not decrypt file"
  exit 1
fi

FILE=$1
KEYS=$(curl https://github.com/ivuorinen.keys)

age -d 
