# x-timestamp

Print the current Unix epoch timestamp in seconds.

## Usage

```bash
x-timestamp
```

Also available as `x timestamp` via the `x` dispatcher.

<!-- vim: set ft=markdown spell spelllang=en_us cc=80 : -->
