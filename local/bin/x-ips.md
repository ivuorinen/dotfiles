# x-ips

List this machine's local IPv4/IPv6 addresses by parsing `ifconfig`.

## Usage

```bash
x-ips
```

Also available as `x ips` via the `x` dispatcher.

<!-- vim: set ft=markdown spell spelllang=en_us cc=80 : -->
