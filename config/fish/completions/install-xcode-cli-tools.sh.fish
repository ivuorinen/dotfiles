# @generated by usage-cli from /Users/ivuorinen/.dotfiles/scripts/install-xcode-cli-tools.sh.usage.kdl

# if "usage" is not installed show an error
if ! type -p usage &>/dev/null
    echo >&2
    echo "Error: usage CLI not found. This is required for completions to work in install-xcode-cli-tools.sh." >&2
    echo "See https://usage.jdx.dev for more information." >&2
    return 1
end

set _usage_spec_install_xcode_cli_tools_sh 'name install-xcode-cli-tools.sh
bin install-xcode-cli-tools.sh
about "Install Xcode Command Line Tools (macOS)"
'
set -l tmpdir (if set -q TMPDIR; echo $TMPDIR; else; echo /tmp; end)
set -l spec_file "$tmpdir/usage__usage_spec_install_xcode_cli_tools_sh.spec"
echo $_usage_spec_install_xcode_cli_tools_sh >"$spec_file"

set -l tokens
if commandline -x >/dev/null 2>&1
    complete -xc install-xcode-cli-tools.sh -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -xpc) (commandline -t))"
else
    complete -xc install-xcode-cli-tools.sh -a "(command usage complete-word --shell fish -f \"$spec_file\" -- (commandline -opc) (commandline -t))"
end
