#compdef x-sha256sum-matcher
# @generated by usage-cli from /Users/ivuorinen/.dotfiles/local/bin/x-sha256sum-matcher.usage.kdl
local curcontext="$curcontext"

_x_sha256sum_matcher() {
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in x-sha256sum-matcher." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_file="${TMPDIR:-/tmp}/usage__usage_spec_x_sha256sum_matcher.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name x-sha256sum-matcher
bin x-sha256sum-matcher
about "Compare two files by SHA256 hash"
flag "-v --verbose" help="Verbose output"
flag "-h --help" help="Show help"
arg <file1> help="First file"
arg <file2> help="Second file"
__USAGE_EOF__
  local -a completions=()
  while IFS= read -r line; do
    completions+=("$line")
  done < <(command usage complete-word --shell zsh -f "$spec_file" -- "${words[@]}")
  _describe 'completions' completions -S ''
  return 0
}

if [ "$funcstack[1]" = "_x_sha256sum_matcher" ]; then
    _x_sha256sum_matcher "$@"
else
    compdef _x_sha256sum_matcher x-sha256sum-matcher
fi

# vim: noet ci pi sts=0 sw=4 ts=4
