#compdef x-term-colors
# @generated by usage-cli from /Users/ivuorinen/.dotfiles/local/bin/x-term-colors.usage.kdl
local curcontext="$curcontext"

_x_term_colors() {
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in x-term-colors." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_file="${TMPDIR:-/tmp}/usage__usage_spec_x_term_colors.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name x-term-colors
bin x-term-colors
about "Display 24-bit terminal color test"
__USAGE_EOF__
  local -a completions=()
  while IFS= read -r line; do
    completions+=("$line")
  done < <(command usage complete-word --shell zsh -f "$spec_file" -- "${words[@]}")
  _describe 'completions' completions -S ''
  return 0
}

if [ "$funcstack[1]" = "_x_term_colors" ]; then
    _x_term_colors "$@"
else
    compdef _x_term_colors x-term-colors
fi

# vim: noet ci pi sts=0 sw=4 ts=4
