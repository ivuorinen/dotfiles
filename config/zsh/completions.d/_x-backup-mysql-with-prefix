#compdef x-backup-mysql-with-prefix
# @generated by usage-cli from /Users/ivuorinen/.dotfiles/local/bin/x-backup-mysql-with-prefix.usage.kdl
local curcontext="$curcontext"

_x_backup_mysql_with_prefix() {
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in x-backup-mysql-with-prefix." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_file="${TMPDIR:-/tmp}/usage__usage_spec_x_backup_mysql_with_prefix.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name x-backup-mysql-with-prefix
bin x-backup-mysql-with-prefix
about "Backup MySQL tables matching a prefix"
arg <table_prefix> help="Table name prefix to match"
arg <filename_prefix> help="Output filename prefix"
arg "[database]" help="Database name (optional)" required=#false
__USAGE_EOF__
  local -a completions=()
  while IFS= read -r line; do
    completions+=("$line")
  done < <(command usage complete-word --shell zsh -f "$spec_file" -- "${words[@]}")
  _describe 'completions' completions -S ''
  return 0
}

if [ "$funcstack[1]" = "_x_backup_mysql_with_prefix" ]; then
    _x_backup_mysql_with_prefix "$@"
else
    compdef _x_backup_mysql_with_prefix x-backup-mysql-with-prefix
fi

# vim: noet ci pi sts=0 sw=4 ts=4
