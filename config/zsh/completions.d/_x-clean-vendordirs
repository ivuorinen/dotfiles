#compdef x-clean-vendordirs
# @generated by usage-cli from /Users/ivuorinen/.dotfiles/local/bin/x-clean-vendordirs.usage.kdl
local curcontext="$curcontext"

_x_clean_vendordirs() {
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in x-clean-vendordirs." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_file="${TMPDIR:-/tmp}/usage__usage_spec_x_clean_vendordirs.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name x-clean-vendordirs
bin x-clean-vendordirs
about "Remove vendor and node_modules directories recursively"
arg "[directory]" help="Directory to clean (default: current)" required=#false
__USAGE_EOF__
  local -a completions=()
  while IFS= read -r line; do
    completions+=("$line")
  done < <(command usage complete-word --shell zsh -f "$spec_file" -- "${words[@]}")
  _describe 'completions' completions -S ''
  return 0
}

if [ "$funcstack[1]" = "_x_clean_vendordirs" ]; then
    _x_clean_vendordirs "$@"
else
    compdef _x_clean_vendordirs x-clean-vendordirs
fi

# vim: noet ci pi sts=0 sw=4 ts=4
