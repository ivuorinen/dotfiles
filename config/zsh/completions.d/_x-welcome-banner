#compdef x-welcome-banner
# @generated by usage-cli from /Users/ivuorinen/.dotfiles/local/bin/x-welcome-banner.usage.kdl
local curcontext="$curcontext"

_x_welcome_banner() {
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in x-welcome-banner." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_file="${TMPDIR:-/tmp}/usage__usage_spec_x_welcome_banner.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name x-welcome-banner
bin x-welcome-banner
about "Print system welcome banner with host info"
__USAGE_EOF__
  local -a completions=()
  while IFS= read -r line; do
    completions+=("$line")
  done < <(command usage complete-word --shell zsh -f "$spec_file" -- "${words[@]}")
  _describe 'completions' completions -S ''
  return 0
}

if [ "$funcstack[1]" = "_x_welcome_banner" ]; then
    _x_welcome_banner "$@"
else
    compdef _x_welcome_banner x-welcome-banner
fi

# vim: noet ci pi sts=0 sw=4 ts=4
