#compdef x-gh-get-latest-release-targz
# @generated by usage-cli from /Users/ivuorinen/.dotfiles/local/bin/x-gh-get-latest-release-targz.usage.kdl
local curcontext="$curcontext"

_x_gh_get_latest_release_targz() {
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in x-gh-get-latest-release-targz." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_file="${TMPDIR:-/tmp}/usage__usage_spec_x_gh_get_latest_release_targz.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name x-gh-get-latest-release-targz
bin x-gh-get-latest-release-targz
about "Download latest GitHub release tarball"
flag --get help="Download the release"
arg <repo> help="GitHub repository (owner/repo)"
__USAGE_EOF__
  local -a completions=()
  while IFS= read -r line; do
    completions+=("$line")
  done < <(command usage complete-word --shell zsh -f "$spec_file" -- "${words[@]}")
  _describe 'completions' completions -S ''
  return 0
}

if [ "$funcstack[1]" = "_x_gh_get_latest_release_targz" ]; then
    _x_gh_get_latest_release_targz "$@"
else
    compdef _x_gh_get_latest_release_targz x-gh-get-latest-release-targz
fi

# vim: noet ci pi sts=0 sw=4 ts=4
