#compdef x-path-remove
# @generated by usage-cli from /Users/ivuorinen/.dotfiles/local/bin/x-path-remove.usage.kdl
local curcontext="$curcontext"

_x_path_remove() {
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in x-path-remove." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_file="${TMPDIR:-/tmp}/usage__usage_spec_x_path_remove.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name x-path-remove
bin x-path-remove
about "Remove directories from PATH"
arg <directory>… help="Directory to remove" var=#true
__USAGE_EOF__
  local -a completions=()
  while IFS= read -r line; do
    completions+=("$line")
  done < <(command usage complete-word --shell zsh -f "$spec_file" -- "${words[@]}")
  _describe 'completions' completions -S ''
  return 0
}

if [ "$funcstack[1]" = "_x_path_remove" ]; then
    _x_path_remove "$@"
else
    compdef _x_path_remove x-path-remove
fi

# vim: noet ci pi sts=0 sw=4 ts=4
