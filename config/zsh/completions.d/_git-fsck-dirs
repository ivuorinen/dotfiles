#compdef git-fsck-dirs
# @generated by usage-cli from /Users/ivuorinen/.dotfiles/local/bin/git-fsck-dirs.usage.kdl
local curcontext="$curcontext"

_git_fsck_dirs() {
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in git-fsck-dirs." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_file="${TMPDIR:-/tmp}/usage__usage_spec_git_fsck_dirs.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name git-fsck-dirs
bin git-fsck-dirs
about "Run git fsck on all git repositories in subdirectories"
__USAGE_EOF__
  local -a completions=()
  while IFS= read -r line; do
    completions+=("$line")
  done < <(command usage complete-word --shell zsh -f "$spec_file" -- "${words[@]}")
  _describe 'completions' completions -S ''
  return 0
}

if [ "$funcstack[1]" = "_git_fsck_dirs" ]; then
    _git_fsck_dirs "$@"
else
    compdef _git_fsck_dirs git-fsck-dirs
fi

# vim: noet ci pi sts=0 sw=4 ts=4
