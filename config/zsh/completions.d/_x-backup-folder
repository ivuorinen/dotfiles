#compdef x-backup-folder
# @generated by usage-cli from /Users/ivuorinen/.dotfiles/local/bin/x-backup-folder.usage.kdl
local curcontext="$curcontext"

_x_backup_folder() {
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in x-backup-folder." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_file="${TMPDIR:-/tmp}/usage__usage_spec_x_backup_folder.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name x-backup-folder
bin x-backup-folder
about "Backup a folder with a timestamp"
arg <folder> help="Folder to backup"
arg "[filename]" help="Custom output filename (default: folder name)" required=#false
__USAGE_EOF__
  local -a completions=()
  while IFS= read -r line; do
    completions+=("$line")
  done < <(command usage complete-word --shell zsh -f "$spec_file" -- "${words[@]}")
  _describe 'completions' completions -S ''
  return 0
}

if [ "$funcstack[1]" = "_x_backup_folder" ]; then
    _x_backup_folder "$@"
else
    compdef _x_backup_folder x-backup-folder
fi

# vim: noet ci pi sts=0 sw=4 ts=4
