#compdef x-ssl-expiry-date
# @generated by usage-cli from /Users/ivuorinen/.dotfiles/local/bin/x-ssl-expiry-date.usage.kdl
local curcontext="$curcontext"

_x_ssl_expiry_date() {
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in x-ssl-expiry-date." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_file="${TMPDIR:-/tmp}/usage__usage_spec_x_ssl_expiry_date.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name x-ssl-expiry-date
bin x-ssl-expiry-date
about "Check SSL certificate expiry date"
flag -p help="Port number (default: 443)" {
    arg <port>
}
flag -d help="Days threshold for expiry warning" {
    arg <days>
}
arg <hostname> help="Domain or hostname to check"
__USAGE_EOF__
  local -a completions=()
  while IFS= read -r line; do
    completions+=("$line")
  done < <(command usage complete-word --shell zsh -f "$spec_file" -- "${words[@]}")
  _describe 'completions' completions -S ''
  return 0
}

if [ "$funcstack[1]" = "_x_ssl_expiry_date" ]; then
    _x_ssl_expiry_date "$@"
else
    compdef _x_ssl_expiry_date x-ssl-expiry-date
fi

# vim: noet ci pi sts=0 sw=4 ts=4
