#compdef x-dfm-docs-xterm-keybindings
# @generated by usage-cli from /Users/ivuorinen/.dotfiles/local/bin/x-dfm-docs-xterm-keybindings.usage.kdl
local curcontext="$curcontext"

_x_dfm_docs_xterm_keybindings() {
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in x-dfm-docs-xterm-keybindings." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_file="${TMPDIR:-/tmp}/usage__usage_spec_x_dfm_docs_xterm_keybindings.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name x-dfm-docs-xterm-keybindings
bin x-dfm-docs-xterm-keybindings
about "Update tmux keybindings documentation"
__USAGE_EOF__
  local -a completions=()
  while IFS= read -r line; do
    completions+=("$line")
  done < <(command usage complete-word --shell zsh -f "$spec_file" -- "${words[@]}")
  _describe 'completions' completions -S ''
  return 0
}

if [ "$funcstack[1]" = "_x_dfm_docs_xterm_keybindings" ]; then
    _x_dfm_docs_xterm_keybindings "$@"
else
    compdef _x_dfm_docs_xterm_keybindings x-dfm-docs-xterm-keybindings
fi

# vim: noet ci pi sts=0 sw=4 ts=4
