#compdef x-env-list
# @generated by usage-cli from /Users/ivuorinen/.dotfiles/local/bin/x-env-list.usage.kdl
local curcontext="$curcontext"

_x_env_list() {
  typeset -A opt_args
  local curcontext="$curcontext" cache_policy

  if ! type -p usage &> /dev/null; then
      echo >&2
      echo "Error: usage CLI not found. This is required for completions to work in x-env-list." >&2
      echo "See https://usage.jdx.dev for more information." >&2
      return 1
  fi

  local spec_file="${TMPDIR:-/tmp}/usage__usage_spec_x_env_list.spec"
  cat >| "$spec_file" <<'__USAGE_EOF__'
name x-env-list
bin x-env-list
about "List environment variables grouped by prefix"
__USAGE_EOF__
  local -a completions=()
  while IFS= read -r line; do
    completions+=("$line")
  done < <(command usage complete-word --shell zsh -f "$spec_file" -- "${words[@]}")
  _describe 'completions' completions -S ''
  return 0
}

if [ "$funcstack[1]" = "_x_env_list" ]; then
    _x_env_list "$@"
else
    compdef _x_env_list x-env-list
fi

# vim: noet ci pi sts=0 sw=4 ts=4
